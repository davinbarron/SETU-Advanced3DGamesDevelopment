# CA2 Learning Journal

## Section 1 — Weekly Evidence Log

The dates shown here in my learning log capture the truthful commits logged throughout the semester from my repo. These differ from the provided example from the rubric which shows each week in order from 1-9. Here I want to show real work done and when I did it e.g instead of including the week 5 lab in week 5 I have it as week 8 since that is when I did the work. However I have broken them up into the lab sessions and the CA2 feature implemntation work for readability.

| Week | Dates | What I worked on | Key commits / tags | Evidence references | Blocker / resolution |
|---|---|---|---|---|---|
| 3 | 2–9 Feb 2026 | Unity project setup, demo scene created, radioactive barrel modelled and textured in Substance Painter with PBR maps exported for Unity. | `de89f6f Unity project setup`, `7cb0e0c merge: demo scene implementation`, `a259607 feat: barrel in substance painter` | `Docs/CA1.docx` baseline screenshots | Metallic value set incorrectly in Substance Painter causing barrel to appear black in Unity — corrected and documented. |
| 4 | 10–14 Feb 2026 | Shader Graph dissolve effect built and scripted. Voronoi noise, burn band, emission colour controls exposed. C# driver script using `MaterialPropertyBlock`. | `f780b33 feat: shader graph dissolve complete`, `def6e34 feat: dissolve script`, `e109884 feat: pulsing effect` | `Docs/CA1.docx` shader sections | Voronoi remap values caused dissolve artefacts at amount 0 — corrected remap range resolved it. |
| 4 | 15–16 Feb 2026 | Unreal comparison: replicated dissolve logic in UE5 Material Editor. Lighting pass in Unity — dusk outdoor scene with motivated lights. Post processing volume configured. CA1 packaged. | `071acb9 feat: dissolve in unreal`, `7cc98f3 feat: ambient dusk lighting`, `5bcf922 feat: post processing`, `fa2bb37 merge: post processing`, `8f3749d merge: unreal comparison` | `Docs/CA1.docx` lighting and Unreal comparison sections | Bloom too aggressive initially — reduced intensity and documented the trade-off. |
| 8 | 20–22 March 2026 | W5-A rendering profiling (Forward vs Deferred, overdraw, draw-call good/bad). W5-B constraints baseline: authored fan LODs in Blender, configured Unity LOD Group, baked occlusion, captured 3-camera stats. | `6607ae5 perf: profile forward vs deferred`, `fba1c3a perf: LOD profiling`, `cf34910 feat: w5-b LOD setup`, `4a1e819 merge: w5-b lod-occlusion` | `Docs/Rendering Profiling Pack.docx`, `Docs/constraints.md`, `Docs/Screenshots/W5-B/*` | Fan asset had single mesh only; generated `LOD1/LOD2` via Blender Decimate and re-imported FBX with LOD naming. |
| 9 | 23–24 Mar 2026 | W6-A Nanite awareness (virtualised geometry, overdraw and metrics evidence). W6-B Azure baseline: created/published Auth function and configured Photon with function URL. | `0b66531 docs: nanite awareness`, `d16489a feat: auth azure function`, `d4c8953 docs: azure function config with photon`, `494cdb9 merge: week6-b` | `Docs/nanite_awareness.md`, `Docs/w6-b_azure_function_baseline.md`, `Docs/Screenshots/W6-A/*`, `Docs/Screenshots/W6-B/*` | Azure dashboard UI was confusing to navigate and understand what availablity zones were available with my student account. |
| 9 | 24–25 Mar 2026 | W7-A Fusion KCC session. Imported Fusion SDK and KCC assets, set up network sandbox scene, achieved two-client baseline with player spawning. | `bbb3ca1 feat: importing fusion sdk`, `58e5e70 net: working multiplayer lobby`, `991e6e8 feat: network sample scene setup`, `799a906 merge: week7-a` | `Docs/Screenshots/W7-A/*` | No blockers. |
| 9 | 27–28 Mar 2026 | W7-B Unity Authentication integration: disabled anonymous Photon access, added Unity Player Account sign-in, passed auth token to Photon custom auth, and confirmed two authenticated players spawning in the same room. | `8ffaa43 net: auth sign in works`, `bd8ea74 net: unity service auth`, `82b4b1d merge: week7-b custom unity auth`, `4736ac4 docs: W7-B` | `Docs/Screenshots/W7-B/*`, `Docs/w7-b_unity_authentication.md` | `GameSpawner` must live on the NetworkRunner prefab not a scene GameObject for `IPlayerJoined` to fire. Photon 401 error — Azure function URL was missing `?code=KEY`. |
| 9 | 28–29 Mar 2026 | W8-A Lobby system and room browser: implemented `LobbyManager`, `RoomBrowserUI`, `RoomListItem`, and `PlayerNameTag` so players can create and join named rooms with a networked display name shown above each character. W8-B RPC emote system: added three emote actions (Wave, Cheer, Taunt) triggered by keys 1–3, fired via `Rpc_PlayEmote` from `InputAuthority` to all peers, displaying a timed speech bubble above the player using `PlayerNameTag`. | `23b3670 net: sample scripts + updates`, `8fd4c3e merge: week8-a lobby`, `0bd7503 net: rpc player emotes`, `11002b6 merge: week8-b emotes`, `9bcc1de docs: CA2 Plan`, `0b63460 docs: A1 baseline` | `Docs/Screenshots/W8-A/*`, `Docs/Screenshots/W8-B/*`, `Docs/w8-a_lobby_room_browser.md`, `Docs/w8-b_rpc_emotes.md` | `EventSystem` must exist in the scene for UI clicks to register — added auto-creation in `RoomBrowserUI`. `BuildEmoteBubble()` must be called in `Spawned()` before any RPC fires. |
| 10 | 1–3 Apr 2026 | CA2 feature implementation: refactored networking to align with KCC sample scripts, networked countdown timer with phase system (Option C), score orbs with authoritative pickup and respawn (Option B), win state with final player rankings. | `e73acdc feat: code refactoring`, `3e25c36 fix: removing old player spawner`, `0ed8ca3 net: countdown timer`, `82ce8ee feat: pulsing timer`, `6b7d6fa net: score orbs`, `b9d4804 net: score HUD`, `8ca01ba net: win state and rankings` | `Docs/Screenshots/CA2/*` | `Rpc_CastVote` with `RpcSources.InputAuthority` threw "Local simulation not allowed" error on `GameStateManager` — changed to `RpcSources.All` with internal `HasStateAuthority` guard. |
| 10 | 4–5 Apr 2026 | CA2 finalisation: UI helpers extracted (`UIHelper`), rematch vote system, score reset on rematch, skip re-auth on lobby return, test matrix with known issues, Unreal terminology note, Learning Journal completed, `ca2-submit` tag applied. | `3b38552 feat: UI helpers`, `4fb2c36 net: rematch vote`, `f3e252d net: score reset`, `93240dc net: skip re-auth`, `a28ea6d docs: test matrix`, `ca2-submit` tag | `Docs/CA2_TestMatrix.md`, `Docs/CA2_UnrealTerminology.md`, `Docs/CA2_BranchStrategy.md` | Host client leaving room leaves remaining client stuck in frozen phase — StateAuthority migration not implemented, documented as KI-1 in test matrix. |

---

## Section 2 — Critical Discussion of Technical Choices

### 1. Why I chose this feature scope

The big deciding factor for the features I decided to implement for this CA was the lab work. By Week 8 the foundation already included a working Fusion Shared Mode session with KCC character movement, Unity Player Account authentication, a lobby browser, room creation and joining, player name tags, and an RPC emote system. It was only a matter of using this foundation and building up from it. This is why I decided to go for Options B and C since they both fit naturally into the Shared Mode authority model we had already been working with.

I tackled Option C first, a networked countdown timer, since it felt like a manageable starting point that could immediately demonstrate the `[Networked]` property pattern across two clients. From there I added Option B, a scoring system using collectible orbs, which then enhanced Option C by giving the round a meaningful outcome, a win state based on who collected the most points.

I deliberately left out Options A and D. Option A, server-authoritative character movement, would have required switching from Shared Mode to a server or host topology, which would have meant reimplementing the session management and authority model from scratch. Option D, networked projectiles, has similar requirements and introduces hit detection complexity that wasn't covered in the labs. Both were technically interesting but would have taken longer than the time allowed and risked producing something unstable rather than a smaller, well-tested feature.

### 2. [Networked] property vs RPC

I made use of several `[Networked]` properties and RPCs in this project, and the distinction between the two became clear through implementation.

`[Networked]` properties are used for persistent state — values that any client needs to be correct at any point regardless of when they joined the session. `TimeRemaining`, `Phase`, `Winner`, `RematchVotes`, and `Score` are all persistent because a client joining mid-round must immediately see the correct timer, the correct phase, and the correct scores without any event needing to be replayed. Fusion includes these in the initial state snapshot automatically so late joiners are never out of sync.

RPCs are the right tool for one-shot events that do not need to be preserved. `Rpc_GameOver` starts once when the timer hits zero — a late-joining client does not need to receive it because `Phase == GameOver` in the state snapshot already communicates that the round has ended. `Rpc_PlayEmote` starts once per keypress — there is no meaningful "current emote" state to store. If I had used a `[Networked]` string for the emote I would have needed timer logic to clear it and would have been treating an event as if it were state, which would have been semantically wrong and added unnecessary complexity.

From this I designed `Rpc_CastVote`. It uses `RpcSources.All, RpcTargets.StateAuthority` where any client can call it but only the StateAuthority processes it. This keeps vote counting authoritative and mirrors the Server RPC pattern from Unreal Engine, where clients send requests upward to the server for authoritative processing rather than writing state themselves.

### 3. A concrete authority model decision

During the initial scene setup, an early version of the player prefab had a Camera component attached to it. When a second player joined the session, both player objects existed in each client's scene and both `LateUpdate()` calls were trying to move `Camera.main` to their respective `CameraHandle` positions every frame. The result was that the camera flickered rapidly between the two positions, making the game completely unplayable for both clients.

I fixed this by removing the Camera component from the player prefab entirely and keep a single `Main Camera` in the scene. The `LateUpdate()` in `Player.cs` already had a `HasInputAuthority` guard so only the local player's object is allowed to move the camera. With the camera living in the scene rather than on the prefab, only one client ever repositions it per frame, and the remote player's object correctly skips `LateUpdate()` because it does not have `HasInputAuthority`.

### 4. One thing I would do differently

The commit `e73acdc feat: code refactoring to follow lab examples provided` shows a significant rework that I could have avoided with better planning. When I first implemented the networking layer I wrote a custom `NetworkManager` script based on what I understood from the lab videos without realising that the KCC sample package already included `GameplayManager` for player spawning and that `FusionBootstrap` could be modified directly to handle authentication. Both my `NetworkManager` and my `PlayerSpawner` were essentially duplicates of scripts that already existed. Replacing them was a bit time consuming so I wish I had used the provided content from the get go.

---

## Section 3 — Lessons Learned

- I would say one of my biggest regrets would be not starting the CA sooner. I am pretty happy with the work I managed to produce for this CA but it would've been better to not have to give myself extra pressure of leaving things late especially since I should've been commiting regularly each week. However, I did complete the lab work before starting the CA and I did find it really helpful since it gave me a nice baseline to work with and actually made the CA a lot easier in the process. I hope to keep up with the lab content going into CA3.

- I found that `IPlayerJoined` only starts on `SimulationBehaviour` components attached to the NetworkRunner prefab, not on scene GameObjects. `GameSpawner` was placed on a scene GameObject initially and `PlayerJoined` never started despite the session connecting correctly. Moving the component to the NetworkRunner prefab resolved it immediately. Here I learned that anything that needs to respond to Fusion session events must live on the runner.

- Initially I had an issue with authentciating using my custom auth URL on the Photon dashboard. The Photon dashboard custom auth URL must include `?code=KEY` appended directly to the URL. This cost a full session to diagnose. My Azure Function Monitor showing zero invocations was a big sign that it must not have been invoked at all, if the function is never called, the problem is in the URL configuration, not the function code itself.